---
name: where-to-eat
description: 多人聚餐地点和餐厅推荐。当用户想约饭、寻找公平会合地点或按口味找餐厅时使用。基于参与者的真实出发地、实际路线耗时和美食偏好，筛选并解释三家可执行的候选餐厅。
version: 2.1.0
---

# 聚餐地点和餐厅推荐

这个 Skill 的最终目标是推荐“去哪里吃”，而不是只计算一个抽象的地理中心。会合区域、交通线路和公平指标都是筛选餐厅的证据。

## 适用边界

- 本文件用于 Codex/MCP 的对话式流程，调用已配置的高德地图 MCP 工具。
- `web/` 与 `functions/` 是独立的网页服务：Pages Functions 调用高德 REST API，浏览器不直连高德，也不调用 MCP。
- 目前只接受北京、上海、广州、深圳；所有参与者必须属于同一座城市。
- 需要 2–5 位参与者。餐厅按用户口味搜索，不能只按评分排序。

## 决策原则

1. 先保证“每个人能实际到店”，再谈公平；路线缺失或地点不在所选城市时，不得用猜测补齐。
2. 目标是餐厅。重心或交通平衡区域只用于生成搜索种子，不能直接当成最终推荐地点。
3. 默认公平策略按以下顺序执行：
   - `max`：最慢的人更快（先最小化所有人的最大耗时）；
   - `spread`：时间差最小（最大耗时减最小耗时）；
   - `average`：平均耗时最低。
   用户切换策略后，使用该策略排序，并用另外两个指标作为同分时的次级排序。
4. 默认输出 1 家“推荐方案”加最多 2 家“备选方案”。推荐方案只是当前策略下的最优解，不代表用户不能选择其他餐厅。
5. 所有时间和路线必须标明来源：高德 API 实时结果，或明确标为“估算”。不能把估算写成精确分钟数。

## 收集输入

至少确认：城市、每个人的姓名（可选）和出发地、口味（可选但建议填写）。可询问聚餐到达时间；没有指定时使用“现在查询/现在出发”的语义，不虚构未来高峰预测。

可选输入：出行方式（公共交通、驾车、自行车）、预算、忌口和其他约束。用户明确排除某种方式时，后续结果不得出现该方式。若用户没有选择方式，至少计算公共交通；在 API 支持且成本可接受时同时计算驾车和自行车，最后按实际完整路线选出每家餐厅的最佳方式。

## 出发地解析规则

对每个出发地调用 `mcp_tool_amap-maps_maps_geo`，并保留“用户原文”和“最终解析结果”。

- 明确写出“地铁站/公交站/小区/商场/街道/路/号”等类型时，按用户指定类型解析。
- 只有“望京”“霍营”“来广营”这类地名时，按以下顺序尝试：
  1. `<原文>地铁站`；
  2. 若没有可验证的地铁站，再尝试 `<原文>公交站`；
  3. 两者都没有，才查询原始地名。
- 只有返回结果的地点级别明确包含“公交地铁站点”时，才可接受为站点；不能因为名字相似就假定它是车站。
- 结果中必须显示：`原始输入 → 实际使用地点`。如果解析到公交站或原始地点，明确说明回退原因，并允许用户修正。
- 检查 `adcode`/城市字段，拒绝不在所选城市的地点。无法定位时，要求用户补充“地铁站、小区、商圈或完整地址”，不要静默使用错误坐标。

## 计算候选餐厅

1. 对参与者坐标计算一个重心，并可用 `mcp_tool_amap-maps_maps_regeocode` 得到可读区域名。重心只是搜索参考。
2. 以重心及其附近的 2 个交通平衡种子区域调用 `mcp_tool_amap-maps_maps_text_search`，传入用户的菜系/餐厅关键词和城市。
3. 合并并按 POI id、名称和坐标去重。先保留最多 9 个有口味匹配、地址和坐标的候选；详情工具调用最多 9 次，避免无边界消耗配额。
4. 可调用 `mcp_tool_amap-maps_maps_search_detail` 补充地址、评分、人均、营业信息，但评分只能作为可用性和同分时的辅助因素，不能覆盖路线公平性或用户口味。

## 真实路线与评分

对每个候选餐厅、每位参与者、每个允许的出行方式调用对应路线工具：

- 驾车：`mcp_tool_amap-maps_maps_direction_driving`
- 公共交通：`mcp_tool_amap-maps_maps_direction_transit_integrated`
- 自行车：`mcp_tool_amap-maps_maps_direction_bicycling`（若 MCP 没有该工具，明确说明不可用）

每个候选必须先有所有参与者的完整路线，才可进入排序。记录每个人的总耗时、出行方式和路线文本，并计算：

```text
max    = max(每个人耗时)
min    = min(每个人耗时)
spread = max - min
avg    = sum(每个人耗时) / 人数
```

按当前策略排序：`max` 先比较 max，再比较 spread、avg；`spread` 先比较 spread，再比较 max、avg；`average` 先比较 avg，再比较 max、spread。评分、距离、营业状态只用于同分时决胜或过滤明显不可用的 POI。

## 路线说明要求

不要只输出“地铁 30 分钟”。公共交通路线必须从 API 的 segments 中提取并串成可执行说明，至少包含：

- 从哪个站上车；
- 乘坐哪条线路；
- 在哪一站换乘、换乘哪条线路；
- 在哪一站下车；
- 下车后的步行或接驳。

例如：`望京站乘地铁 15 号线至大屯路东站，换乘 5 号线至北苑路北站，步行约 8 分钟到店`。API 没有返回换乘站时，不得自行编造；应写“高德未返回完整换乘站信息”，并提供高德路线链接或原始路线摘要。

API 返回的 `duration` 已是该路线的总耗时，包含其返回的步行、候车和换乘部分。不要再次人为加上固定的“每次换乘 3–5 分钟”，否则会重复计算。

## 到达时间与出发建议

如果用户提供到达时间，用实际查询到的耗时计算建议出发时间，并预留 5 分钟缓冲：

```text
建议出发时间 = 到达时间 - 路线耗时 - 5 分钟
```

把它标成“建议”，不是高德对未来时段的保证。除非所用 API 明确支持目标日期/时间，否则不要声称已经模拟了晚高峰。没有到达时间时，显示“现在出发，约 X 分钟到店”。

## 输出格式

输出应短而可核验，顺序建议如下：

1. **推荐餐厅**：餐厅名、口味匹配、推荐策略、推荐出行方式、平均/最慢/时间差。
2. **每个人的路线**：原始出发地 → 解析地点、耗时、完整线路、建议出发时间（如有）。
3. **为什么推荐**：说明它在当前策略下如何优于另外候选；不要只写“评分最高”。
4. **备选餐厅**：最多两家，默认折叠式地列出同样的关键指标和路线摘要。
5. **数据说明**：高德 API 时间或估算、查询时间、可能的拥堵/候车波动。

先展示推荐方案，备选方案供用户比较；不要默认把所有餐厅的所有路线细节一次性铺满。用户要求比较时，再展开指定候选。

## 错误与降级

- 地理编码失败：说明哪个输入失败，要求补充地址；不使用附近猜测点。
- 某条路线失败：该出行方式标为不可用；若候选无法让所有人完成路线，则淘汰该候选。
- 高德限流或工具不可用：可以给出估算区间，但必须标注“估算”、方法和不确定性；不要把估算候选伪装成正式推荐。
- 餐厅搜索为空：先放宽同义口味关键词或扩大搜索半径，并告诉用户发生了什么；仍无结果时请求新的口味/区域约束。
- 用户纠正站名或线路：保留用户原文，同时重新调用 API 验证；验证失败时以 API 结果为准并明确冲突，不盲从或争辩。

## 依赖工具与参考

- 地理编码：`mcp_tool_amap-maps_maps_geo`
- 逆地理编码：`mcp_tool_amap-maps_maps_regeocode`
- 驾车：`mcp_tool_amap-maps_maps_direction_driving`
- 公共交通：`mcp_tool_amap-maps_maps_direction_transit_integrated`
- 骑行：`mcp_tool_amap-maps_maps_direction_bicycling`（可用时）
- 餐厅搜索：`mcp_tool_amap-maps_maps_text_search`
- 餐厅详情：`mcp_tool_amap-maps_maps_search_detail`

需要 API 参数或返回结构时，读取 [references/api-guide.md](references/api-guide.md)；需要重心、指标或排序背景时，读取 [references/algorithm.md](references/algorithm.md)。不要因为用户没有要求而生成或保存 Markdown 文件。
